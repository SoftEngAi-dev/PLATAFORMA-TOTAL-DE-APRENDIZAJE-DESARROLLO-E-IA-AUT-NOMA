# Aplicación web

Experiencia PC/Web del MVP. `index.html` y `app.js` muestran la ruta inicial,
consumen `services/api` cuando está disponible y mantienen un fallback
offline para no bloquear el aprendizaje.
